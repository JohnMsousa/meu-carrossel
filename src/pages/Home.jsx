import React from "react";
import Carousel from "../components/Carousel/Carousel"; // Importa o Carousel

const Home = () => {
  return (
    <div>
      <Carousel /> {/* Renderiza o Carousel aqui */}
    </div>
  );
};

export default Home;
